<div class="row">
  <div class="col-md-6 col-md-offset-3 text-center">
    <% if params[:plan]=='2' %>
      <h1>Pro Account</h1>
      <p>Ovo ti je pro akaunt ma to ti je ne znam ni ja</p>
    <% else %>
      <h1>Basic Account</h1>
      <p>ovo ti je onaj osnovni dođi probaj odi na pro be</p>
    <% end %>
  </div>
  <div class="col-md-6 col-md-offset-3">
    <div class="well">
    <h2>Sign up</h2>
      <% if params[:plan]=='2' %>
        <%= render "pro_form" %>
      <% else %>
        <%= render "basic_form" %>
      <% end %>
      
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
